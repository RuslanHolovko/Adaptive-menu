$('.button').click(function(){
	$('.nav').toggleClass('showMenu');

	var navig = $('.nav');
	var showM = $('.showMenu');

	if(navig += showM){
			$(this).removeClass('.showMenu');
	}else{
		return (null);
	}
});